**Task (capability test):** Extract information **fully and faithfully** from every document provided in this request. The evaluator is testing your ability to **retrieve and preserve** content from all paragraph blocks, not to skip or compress the corpus.

Apply the following to the **Input document** section in this message (under `## Input document (parsed from DOCX)`).

The only source material is **DOCUMENT_METADATA** and **PARAGRAPH_BLOCKS** there. Read every block; do not drop low-signal lines (headers, footers, short lines) unless they are truly empty in the source listing.

## What to produce (must match the configured JSON schema)

1) **overview_markdown** — A thorough reviewer-style overview: what the document(s) are, what each major part appears to cover, key facts and entities you can support with the text, and any notable risks, ambiguities, or missing context. Use Markdown headings and bullets. **Do not invent** facts that are not supported by PARAGRAPH_BLOCKS.

2) **content_sheet_rows** — **One row per block** in PARAGRAPH_BLOCKS:
   - **block_id** — must match exactly.
   - **paragraph_index** — must match exactly.
   - **verbatim_text** — **full** text of that block, copied exactly from PARAGRAPH_BLOCKS (no paraphrase, no truncation).
   - **topic_or_heading** — short reviewer label (section, topic, or "(misc)"); use empty string only if truly unclassifiable.

Rules: include **every** block **once**, sorted by **paragraph_index** ascending. Do not merge or add blocks.

Output: **only** structured JSON that conforms to the configured schema.
